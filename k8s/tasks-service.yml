apiVersion: v1
kind: Service
metadata:
  name: tasks-service
spec:
  selector:
    app: tasks
  type: LoadBalancer # Cluster IP by default, internal IP only; NodePort still not stable as nodes are changing (create/delete)
  ports:
    - protocol: TCP
      port: 8000 # outside facing
      targetPort: 8000 # container expected port
